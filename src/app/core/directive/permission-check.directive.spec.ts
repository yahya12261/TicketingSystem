import { PermissionCheckDirective } from './permission-check.directive';

describe('PermissionCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new PermissionCheckDirective();
    expect(directive).toBeTruthy();
  });
});
